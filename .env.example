# Copy to ~/.claude/.env — the SessionStart hook in settings.json reads from this location.
EMPLOYEE_CODE=claude
WORKTREES_DIR=$HOME/.claude/worktrees

# Atlassian (exported by SessionStart hook)
ATLASSIAN_EMAIL=
ATLASSIAN_CLOUD_ID=

# Github Reviewers
GITHUB_REVIEWERS=